server:
  port: 8086
aws:
  region: ${AWS_REGION:us-east-1}
  endpoint: ${AWS_ENDPOINT_URL:http://localhost:4566}
  orders-bucket: ${ORDERS_BUCKET:repaso-orders}
  orders-queue-url: ${ORDERS_QUEUE_URL:http://localhost:4566/000000000000/orders}
  orders-table: ${ORDERS_TABLE:Orders}
